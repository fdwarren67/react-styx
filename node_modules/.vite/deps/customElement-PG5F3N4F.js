import {
  m as m4,
  s as s2
} from "./chunk-7KANSCZF.js";
import {
  m as m3
} from "./chunk-DPQ7TENX.js";
import {
  i as i2,
  q
} from "./chunk-ZHGVMVCT.js";
import {
  B,
  D,
  O,
  W
} from "./chunk-VLJE74YV.js";
import {
  I,
  T,
  v
} from "./chunk-YJUTXVD7.js";
import {
  m,
  p
} from "./chunk-VBBJGZOS.js";
import {
  keyed
} from "./chunk-KZXUV47B.js";
import {
  t
} from "./chunk-A7MNUVGI.js";
import {
  m as m2
} from "./chunk-2GIEDWQ6.js";
import {
  ref
} from "./chunk-LTKIMXCE.js";
import {
  s
} from "./chunk-RZAGQ6CT.js";
import {
  Et,
  It
} from "./chunk-VQ5234AM.js";
import "./chunk-PXZ2RU65.js";
import {
  i
} from "./chunk-S3EVYUV4.js";
import {
  S
} from "./chunk-B7E2AREE.js";
import {
  LitElement,
  createEvent,
  css,
  html,
  nothing,
  safeClassMap,
  safeStyleMap,
  setAttribute,
  stringOrBoolean
} from "./chunk-MALSDV7D.js";
import "./chunk-EWTE5DHJ.js";

// node_modules/@esri/calcite-components/dist/components/calcite-slider/customElement.js
var l = {
  container: "container",
  containerRange: "container--range",
  graph: "graph",
  handle: "handle",
  handleExtension: "handle-extension",
  handleLabel: "handle__label",
  handleLabelMinValue: "handle__label--minValue",
  handleLabelValue: "handle__label--value",
  hyphen: "hyphen",
  hyphenWrap: "hyphen--wrap",
  static: "static",
  thumb: "thumb",
  thumbActive: "thumb--active",
  thumbContainer: "thumb-container",
  thumbMinValue: "thumb--minValue",
  thumbPrecise: "thumb--precise",
  thumbValue: "thumb--value",
  tick: "tick",
  tickActive: "tick--active",
  tickLabel: "tick__label",
  tickMax: "tick__label--max",
  tickMin: "tick__label--min",
  ticks: "ticks",
  track: "track",
  trackRange: "track__range",
  transformed: "transformed"
};
var D2 = {
  validationMessage: "validationMessage"
};
var ae = 250;
var ie = css`@charset "UTF-8";:host([disabled]){cursor:default;-webkit-user-select:none;user-select:none;opacity:var(--calcite-opacity-disabled)}:host([disabled]) *,:host([disabled]) ::slotted(*){pointer-events:none}.scale--s{--calcite-slider-handle-size: .625rem;--calcite-slider-handle-extension-height: .4rem;--calcite-slider-container-font-size: var(--calcite-font-size--3)}.scale--s .handle__label,.scale--s .tick__label{line-height:.75rem}.scale--m{--calcite-slider-handle-size: .875rem;--calcite-slider-handle-extension-height: .5rem;--calcite-slider-container-font-size: var(--calcite-font-size--2)}.scale--m .handle__label,.scale--m .tick__label{line-height:1rem}.scale--l{--calcite-slider-handle-size: 1rem;--calcite-slider-handle-extension-height: .65rem;--calcite-slider-container-font-size: var(--calcite-font-size--1)}.scale--l .handle__label,.scale--l .tick__label{line-height:1rem}.handle__label,.tick__label{font-weight:var(--calcite-font-weight-medium);color:var(--calcite-slider-text-color, var(--calcite-color-text-2));font-size:var(--calcite-slider-container-font-size)}:host{display:block}:host .validation-container{padding-block-start:0!important}.container{position:relative;display:block;overflow-wrap:normal;word-break:normal;padding-inline:calc(var(--calcite-slider-handle-size) * .5);padding-block:calc(var(--calcite-slider-handle-size) * .5);margin-block:calc(var(--calcite-slider-handle-size) * .5);margin-inline:0;--calcite-slider-full-handle-height: calc( var(--calcite-slider-handle-size) + var(--calcite-slider-handle-extension-height) );touch-action:none;-webkit-user-select:none;user-select:none}:host([disabled]) .track__range,:host([disabled]) .tick--active{background-color:var(--calcite-color-text-3)}:host([disabled]) ::slotted([calcite-hydrated][disabled]),:host([disabled]) [calcite-hydrated][disabled]{opacity:1}.interaction-container{display:contents}.scale--s .thumb:not(.thumb--precise){--calcite-slider-thumb-y-offset: -.375rem}.scale--m .thumb:not(.thumb--precise){--calcite-slider-thumb-y-offset: -.5rem}.scale--l .thumb:not(.thumb--precise){--calcite-slider-thumb-y-offset: -.55rem}:host([precise]:not([has-histogram])) .container .thumb--value{--calcite-slider-thumb-y-offset: calc(var(--calcite-slider-full-handle-height) * -1)}.thumb-container{position:relative;max-inline-size:100%}.thumb{--calcite-slider-thumb-x-offset: calc(var(--calcite-slider-handle-size) * .5);position:absolute;margin:0;display:flex;cursor:pointer;flex-direction:column;align-items:center;border-style:none;background-color:transparent;padding:0;font-family:inherit;outline:2px solid transparent;outline-offset:2px;transform:translate(var(--calcite-slider-thumb-x-offset),var(--calcite-slider-thumb-y-offset))}.thumb .handle__label{white-space:nowrap}.thumb .handle__label.static,.thumb .handle__label.transformed{position:absolute;inset-block:0px;opacity:0}.thumb .handle__label.hyphen:after{content:"\u2014";display:inline-block;inline-size:1em}.thumb .handle__label.hyphen--wrap{display:flex}.thumb .handle{box-sizing:border-box;border-radius:9999px;outline-color:transparent;background-color:var(--calcite-slider-handle-fill-color, var(--calcite-color-foreground-1));block-size:var(--calcite-slider-handle-size);inline-size:var(--calcite-slider-handle-size);box-shadow:0 0 0 2px var(--calcite-color-text-3) inset;transition:border var(--calcite-internal-animation-timing-medium) ease,background-color var(--calcite-internal-animation-timing-medium) ease,box-shadow var(--calcite-animation-timing) ease}.thumb .handle-extension{inline-size:.125rem;block-size:var(--calcite-slider-handle-extension-height);background-color:var(--calcite-slider-handle-extension-color, var(--calcite-color-text-3))}.thumb:hover .handle{box-shadow:0 0 0 3px var(--calcite-color-brand) inset}.thumb:hover .handle-extension{background-color:var(--calcite-slider-accent-color, var(--calcite-color-brand))}.thumb:focus .handle{outline:2px solid var(--calcite-color-focus, var(--calcite-ui-focus-color, var(--calcite-color-brand)));outline-offset:calc(2px*(1 - (2*clamp(0,var(--calcite-offset-invert-focus),1))))}.thumb:focus .handle-extension{background-color:var(--calcite-slider-accent-color, var(--calcite-color-brand))}.thumb.thumb--minValue{transform:translate(calc(var(--calcite-slider-thumb-x-offset) * -1),var(--calcite-slider-thumb-y-offset))}.thumb.thumb--precise{--calcite-slider-thumb-y-offset: -.125rem}:host([label-handles]) .thumb{--calcite-slider-thumb-x-offset: 50%}:host([label-handles]):host(:not([has-histogram])) .scale--s .thumb:not(.thumb--precise){--calcite-slider-thumb-y-offset: -1.4375rem}:host([label-handles]):host(:not([has-histogram])) .scale--m .thumb:not(.thumb--precise){--calcite-slider-thumb-y-offset: -1.875rem}:host([label-handles]):host(:not([has-histogram])) .scale--l .thumb:not(.thumb--precise){--calcite-slider-thumb-y-offset: -2rem}:host([has-histogram][label-handles]) .handle__label,:host([label-handles]:not([has-histogram])) .thumb--minValue.thumb--precise .handle__label{margin-block-start:.5em}:host(:not([has-histogram]):not([precise])) .handle__label,:host([label-handles]:not([has-histogram])) .thumb--value .handle__label{margin-block-end:.5em}:host([label-handles][precise]):host(:not([has-histogram])) .scale--s .thumb--value{--calcite-slider-thumb-y-offset: -2.075rem}:host([label-handles][precise]):host(:not([has-histogram])) .scale--m .thumb--value{--calcite-slider-thumb-y-offset: -2.75rem}:host([label-handles][precise]):host(:not([has-histogram])) .scale--l .thumb--value{--calcite-slider-thumb-y-offset: -3.0625rem}.thumb:focus .handle,.thumb--active .handle{background-color:var(--calcite-slider-accent-color, var(--calcite-color-brand));box-shadow:0 0 8px #00000029}.thumb:hover.thumb--precise:after,.thumb:focus.thumb--precise:after,.thumb--active.thumb--precise:after{background-color:var(--calcite-slider-accent-color, var(--calcite-color-brand))}.track{position:relative;block-size:.125rem;border-radius:0;background-color:var(--calcite-slider-track-color, var(--calcite-color-border-2));transition:all var(--calcite-internal-animation-timing-medium) ease-in}.track__range{position:absolute;inset-block-start:0px;block-size:.125rem;background-color:var(--calcite-slider-track-fill-color, var(--calcite-color-brand))}.container--range .track__range:hover{cursor:ew-resize}.container--range .track__range:after{position:absolute;inline-size:100%;content:"";inset-block-start:calc(var(--calcite-slider-full-handle-height) * .5 * -1);block-size:calc(var(--calcite-slider-handle-size) + var(--calcite-slider-handle-extension-height))}@media (forced-colors: active){.thumb{outline-width:0;outline-offset:0}.handle{outline:2px solid transparent;outline-offset:2px}.thumb:focus .handle,.thumb .handle-extension,.thumb:hover .handle-extension,.thumb:focus .handle-extension,.thumb:active .handle-extension{background-color:canvasText}.track{background-color:canvasText}.track__range{background-color:highlight}}.tick{position:absolute;block-size:.25rem;inline-size:.125rem;border-width:1px;border-style:solid;background-color:var(--calcite-slider-tick-color, var(--calcite-color-border-input));border-color:var(--calcite-slider-tick-border-color, var(--calcite-color-foreground-1));inset-block-start:-2px;pointer-events:none;margin-inline-start:-.125rem}.tick--active{background-color:var(--calcite-slider-tick-selected-color, var(--calcite-color-brand))}.tick__label{pointer-events:none;margin-block-start:.875rem;display:flex;justify-content:center}.tick__label--min{transition:opacity var(--calcite-animation-timing)}.tick__label--max{transition:opacity var(--calcite-internal-animation-timing-fast)}:host([has-histogram][label-handles]) .tick__label--min,:host([has-histogram][label-handles]) .tick__label--max,:host([has-histogram][precise]) .tick__label--min,:host([has-histogram][precise]) .tick__label--max{font-weight:var(--calcite-font-weight-normal);color:var(--calcite-color-text-3)}.graph{color:var(--calcite-slider-graph-color, var(--calcite-color-foreground-3));block-size:48px}:host([label-ticks][ticks]) .container{padding-block-end:calc(.875rem + var(--calcite-slider-container-font-size))}:host([has-histogram]):host([precise][label-handles]) .container{padding-block-end:calc(var(--calcite-slider-full-handle-height) + 1em)}:host([has-histogram]):host([label-handles]:not([precise])) .container{padding-block-end:calc(var(--calcite-slider-handle-size) * .5 + 1em)}:host([has-histogram]):host([precise]:not([label-handles])) .container{padding-block-end:var(--calcite-slider-full-handle-height)}:host(:not([has-histogram])):host([precise]:not([label-handles])) .container{padding-block-start:var(--calcite-slider-full-handle-height)}:host(:not([has-histogram])):host([precise]:not([label-handles])) .container--range{padding-block-end:var(--calcite-slider-full-handle-height)}:host(:not([has-histogram])):host([label-handles]:not([precise])) .container{padding-block-start:calc(var(--calcite-slider-full-handle-height) + 4px)}:host(:not([has-histogram])):host([label-handles][precise]) .container{padding-block-start:calc(var(--calcite-slider-full-handle-height) + var(--calcite-slider-container-font-size) + 4px)}:host(:not([has-histogram])):host([label-handles][precise]) .container--range{padding-block-end:calc(var(--calcite-slider-full-handle-height) + var(--calcite-slider-container-font-size) + 4px)}.validation-container{display:flex;flex-direction:column;align-items:flex-start;align-self:stretch}:host([scale=m]) .validation-container,:host([scale=l]) .validation-container{padding-block-start:.5rem}:host([scale=s]) .validation-container{padding-block-start:.25rem}::slotted(input[slot=hidden-form-input]){margin:0!important;opacity:0!important;outline:none!important;padding:0!important;position:absolute!important;inset:0!important;transform:none!important;-webkit-appearance:none!important;z-index:-1!important}:host([hidden]){display:none}[hidden]{display:none}`;
function g(L) {
  return Array.isArray(L);
}
var _se = class _se extends LitElement {
  // #endregion
  // #region Lifecycle
  constructor() {
    super(), this.activeProp = "value", this.dragEnd = (e) => {
      this.disabled || (this.removeDragListeners(), this.focusActiveHandle(e.clientX), this.lastDragPropValue != this[this.dragProp] && this.emitChange(), this.dragProp = null, this.lastDragPropValue = null, this.minValueDragRange = null, this.maxValueDragRange = null, this.minMaxValueRange = null);
    }, this.dragUpdate = (e) => {
      if (!this.disabled && (e.preventDefault(), this.dragProp)) {
        const t2 = this.mapToRange(e.clientX || e.pageX);
        if (g(this.value) && this.dragProp === "minMaxValue")
          if (this.minValueDragRange && this.maxValueDragRange && this.minMaxValueRange) {
            const i3 = t2 - this.minValueDragRange, a = t2 + this.maxValueDragRange;
            a <= this.max && i3 >= this.min && a - i3 === this.minMaxValueRange && this.setValue({
              minValue: this.clamp(i3, "minValue"),
              maxValue: this.clamp(a, "maxValue")
            });
          } else
            this.minValueDragRange = t2 - this.minValue, this.maxValueDragRange = this.maxValue - t2, this.minMaxValueRange = this.maxValue - this.minValue;
        else
          this.setValue({ [this.dragProp]: this.clamp(t2, this.dragProp) });
      }
    }, this.formatValue = (e) => (q.numberFormatOptions = {
      locale: this.messages._lang,
      numberingSystem: this.numberingSystem,
      useGrouping: this.groupSeparator
    }, q.localize(e.toString())), this.guid = `calcite-slider-${i()}`, this.messages = s({ name: null }), this.pointerUpDragEnd = (e) => {
      this.disabled || !It(e) || this.dragEnd(e);
    }, this.maxValueDragRange = null, this.minMaxValueRange = null, this.minValueDragRange = null, this.tickValues = [], this.disabled = false, this.fillPlacement = "start", this.groupSeparator = false, this.hasHistogram = false, this.labelHandles = false, this.labelTicks = false, this.max = 100, this.min = 0, this.mirrored = false, this.precise = false, this.required = false, this.scale = "m", this.snap = false, this.status = "idle", this.step = 1, this.validity = {
      valid: false,
      badInput: false,
      customError: false,
      patternMismatch: false,
      rangeOverflow: false,
      rangeUnderflow: false,
      stepMismatch: false,
      tooLong: false,
      tooShort: false,
      typeMismatch: false,
      valueMissing: false
    }, this.value = 0, this.calciteSliderChange = createEvent({ cancelable: false }), this.calciteSliderInput = createEvent({ cancelable: false }), this.listen("pointerdown", this.pointerDownHandler), this.listen("keydown", this.handleKeyDown), this.listen("touchstart", this.handleTouchStart);
  }
  // #endregion
  // #region Public Methods
  /** Sets focus on the component. */
  async setFocus() {
    var _a;
    await m2(this), (_a = this.minHandle ? this.minHandle : this.maxHandle) == null ? void 0 : _a.focus();
  }
  connectedCallback() {
    super.connectedCallback(), this.setMinMaxFromValue(), this.setValueFromMinMax(), v(this), D(this);
  }
  load() {
    g(this.value) || (this.value = this.snap ? this.getClosestStep(this.value) : this.clamp(this.value)), O(this, this.value);
  }
  willUpdate(e) {
    e.has("histogram") && (this.hasHistogram = !!this.histogram), e.has("ticks") && (this.tickValues = this.generateTickValues()), e.has("value") && (this.hasUpdated || this.value !== 0) && this.setMinMaxFromValue(), (e.has("minValue") || e.has("maxValue")) && this.setValueFromMinMax();
  }
  updated() {
    this.labelHandles && (this.adjustHostObscuredHandleLabel("value"), g(this.value) && (this.adjustHostObscuredHandleLabel("minValue"), this.precise && !this.hasHistogram || this.hyphenateCollidingRangeHandleLabels())), this.hideObscuredBoundingTickLabels(), m(this);
  }
  disconnectedCallback() {
    super.disconnectedCallback(), T(this), W(this), this.removeDragListeners();
  }
  // #endregion
  // #region Private Methods
  handleKeyDown(e) {
    const t2 = this.shouldMirror(), { activeProp: i3, max: a, min: s3, pageStep: n, step: o } = this, r = this[i3], { key: c } = e;
    if (t(c)) {
      e.preventDefault();
      return;
    }
    let u;
    if (c === "ArrowUp" || c === "ArrowRight" ? u = r + o * (t2 && c === "ArrowRight" ? -1 : 1) : c === "ArrowDown" || c === "ArrowLeft" ? u = r - o * (t2 && c === "ArrowLeft" ? -1 : 1) : c === "PageUp" ? n && (u = r + n) : c === "PageDown" ? n && (u = r - n) : c === "Home" ? u = s3 : c === "End" && (u = a), isNaN(u))
      return;
    e.preventDefault();
    const d = Number(u.toFixed(s2(o)));
    this.setValue({
      [i3]: this.clamp(d, i3)
    });
  }
  pointerDownHandler(e) {
    if (this.disabled || !It(e))
      return;
    const t2 = e.clientX || e.pageX, i3 = this.mapToRange(t2);
    let a = "value";
    g(this.value) && (i3 >= this.minValue && i3 <= this.maxValue && this.lastDragProp === "minMaxValue" ? a = "minMaxValue" : a = Math.abs(this.maxValue - i3) < Math.abs(this.minValue - i3) || i3 >= this.maxValue ? "maxValue" : "minValue"), this.lastDragPropValue = this[a], this.dragStart(a), this.el.shadowRoot.querySelector(`.${l.thumb}:active`) || this.setValue({ [a]: this.clamp(i3, a) }), this.focusActiveHandle(t2);
  }
  handleTouchStart(e) {
    e.preventDefault();
  }
  buildThumbType(e) {
    const t2 = [e];
    return this.labelHandles && t2.push("labeled"), this.precise && t2.push("precise"), this.hasHistogram && t2.push("histogram"), t2.join("-");
  }
  setValueFromMinMax() {
    const { minValue: e, maxValue: t2 } = this;
    typeof e == "number" && typeof t2 == "number" && (this.value = [e, t2]);
  }
  setMinMaxFromValue() {
    const { value: e } = this;
    g(e) && (this.minValue = e[0], this.maxValue = e[1]);
  }
  onLabelClick() {
    this.setFocus();
  }
  shouldMirror() {
    return this.mirrored && !this.hasHistogram;
  }
  shouldUseMinValue() {
    return g(this.value) ? this.hasHistogram && this.maxValue === 0 || !this.hasHistogram && this.minValue === 0 : false;
  }
  getTickDensity() {
    const e = (this.max - this.min) / this.ticks / ae;
    return e < 1 ? 1 : e;
  }
  generateTickValues() {
    const e = this.ticks ?? 0;
    if (e <= 0)
      return [];
    const t2 = [this.min], i3 = this.getTickDensity(), a = e * i3;
    let s3 = this.min;
    for (; s3 < this.max; )
      s3 += a, t2.push(Math.min(s3, this.max));
    return t2.includes(this.max) || t2.push(this.max), t2;
  }
  onThumbBlur() {
    this.activeProp = null;
  }
  onThumbFocus(e) {
    const t2 = e.currentTarget;
    this.activeProp = t2.getAttribute("data-value-prop");
  }
  onThumbPointerDown(e) {
    const t2 = e.currentTarget;
    this.pointerDownDragStart(e, t2.getAttribute("data-value-prop"));
  }
  onTrackPointerDown(e) {
    this.pointerDownDragStart(e, "minMaxValue");
  }
  pointerDownDragStart(e, t2) {
    It(e) && this.dragStart(t2);
  }
  dragStart(e) {
    this.dragProp = e, this.lastDragProp = this.dragProp, this.activeProp = e, window.addEventListener("pointermove", this.dragUpdate), window.addEventListener("pointerup", this.pointerUpDragEnd), window.addEventListener("pointercancel", this.dragEnd);
  }
  focusActiveHandle(e) {
    this.dragProp === "minValue" ? this.minHandle.focus() : this.dragProp === "maxValue" || this.dragProp === "value" ? this.maxHandle.focus() : this.dragProp === "minMaxValue" && this.getClosestHandle(e).focus();
  }
  emitInput() {
    this.calciteSliderInput.emit();
  }
  emitChange() {
    this.calciteSliderChange.emit();
  }
  removeDragListeners() {
    window.removeEventListener("pointermove", this.dragUpdate), window.removeEventListener("pointerup", this.pointerUpDragEnd), window.removeEventListener("pointercancel", this.dragEnd);
  }
  /**
   * Set prop value(s) if changed at the component level
   *
   * @param {object} values - a set of key/value pairs delineating what properties in the component to update
   */
  setValue(e) {
    let t2;
    if (Object.keys(e).forEach((a) => {
      const s3 = e[a];
      t2 || (t2 = this[a] !== s3), this[a] = s3;
    }), !t2)
      return;
    this.dragProp || this.emitChange(), this.emitInput();
  }
  storeTrackRef(e) {
    this.trackEl = e;
  }
  storeThumbRef(e) {
    if (!e)
      return;
    e.getAttribute("data-value-prop") === "minValue" ? this.minHandle = e : this.maxHandle = e;
  }
  /**
   * If number is outside range, constrain to min or max
   *
   * @param value
   * @param prop
   * @private
   */
  clamp(e, t2) {
    return e = m4(e, this.min, this.max), t2 === "maxValue" && (e = Math.max(e, this.minValue)), t2 === "minValue" && (e = Math.min(e, this.maxValue)), e;
  }
  /**
   * Translate a pixel position to value along the range
   *
   * @param x
   * @private
   */
  mapToRange(e) {
    const t2 = this.max - this.min, { left: i3, width: a } = this.trackEl.getBoundingClientRect(), s3 = (e - i3) / a, n = this.shouldMirror(), o = this.clamp(this.min + t2 * (n ? 1 - s3 : s3)), r = Number(o.toFixed(s2(this.step)));
    return this.snap && this.step ? this.getClosestStep(r) : r;
  }
  /**
   * Get closest allowed value along stepped values
   *
   * @param value
   * @private
   */
  getClosestStep(e) {
    const { max: t2, min: i3, step: a } = this, s3 = new i2(`${Math.floor((e - i3) / a)}`).multiply(`${a}`).add(`${i3}`).toString();
    let n = this.clamp(Number(s3));
    return n > t2 && (n -= a), n;
  }
  getClosestHandle(e) {
    return this.getDistanceX(this.maxHandle, e) > this.getDistanceX(this.minHandle, e) ? this.minHandle : this.maxHandle;
  }
  getDistanceX(e, t2) {
    return Math.abs(e.getBoundingClientRect().left - t2);
  }
  getFontSizeForElement(e) {
    return Number(window.getComputedStyle(e).getPropertyValue("font-size").match(/\d+/)[0]);
  }
  /**
   * Get position of value along range as fractional value
   *
   * @param num
   * @return {number} number in the unit interval [0,1]
   * @private
   */
  getUnitInterval(e) {
    e = this.clamp(e);
    const t2 = this.max - this.min;
    return (e - this.min) / t2;
  }
  adjustHostObscuredHandleLabel(e) {
    const t2 = this.el.shadowRoot.querySelector(`.handle__label--${e}`), i3 = this.el.shadowRoot.querySelector(`.handle__label--${e}.static`), a = this.el.shadowRoot.querySelector(`.handle__label--${e}.transformed`), s3 = i3.getBoundingClientRect(), n = this.getHostOffset(s3.left, s3.right);
    t2.style.transform = `translateX(${n}px)`, a.style.transform = `translateX(${n}px)`;
  }
  hyphenateCollidingRangeHandleLabels() {
    const { shadowRoot: e } = this.el, t2 = this.shouldMirror(), i3 = t2 ? "value" : "minValue", a = t2 ? "minValue" : "value", s3 = e.querySelector(`.handle__label--${i3}`), n = e.querySelector(`.handle__label--${i3}.static`), o = e.querySelector(`.handle__label--${i3}.transformed`), r = this.getHostOffset(n.getBoundingClientRect().left, n.getBoundingClientRect().right), c = e.querySelector(`.handle__label--${a}`), u = e.querySelector(`.handle__label--${a}.static`), d = e.querySelector(`.handle__label--${a}.transformed`), m5 = this.getHostOffset(u.getBoundingClientRect().left, u.getBoundingClientRect().right), v2 = this.getFontSizeForElement(s3), p2 = this.getRangeLabelOverlap(o, d), x = s3, b = v2 / 2;
    if (p2 > 0) {
      if (x.classList.add(l.hyphen, l.hyphenWrap), m5 === 0 && r === 0) {
        let h = p2 / 2 - b;
        h = Math.sign(h) === -1 ? Math.abs(h) : -h;
        const V = this.getHostOffset(o.getBoundingClientRect().left + h - b, o.getBoundingClientRect().right + h - b);
        let y = p2 / 2;
        const $ = this.getHostOffset(d.getBoundingClientRect().left + y, d.getBoundingClientRect().right + y);
        V !== 0 && (h += V, y += V), $ !== 0 && (h += $, y += $), s3.style.transform = `translateX(${h}px)`, o.style.transform = `translateX(${h - b}px)`, c.style.transform = `translateX(${y}px)`, d.style.transform = `translateX(${y}px)`;
      } else if (r > 0 || m5 > 0)
        s3.style.transform = `translateX(${r + b}px)`, c.style.transform = `translateX(${p2 + m5}px)`, d.style.transform = `translateX(${p2 + m5}px)`;
      else if (r < 0 || m5 < 0) {
        let h = Math.abs(r) + p2 - b;
        h = Math.sign(h) === -1 ? Math.abs(h) : -h, s3.style.transform = `translateX(${h}px)`, o.style.transform = `translateX(${h - b}px)`;
      }
    } else
      x.classList.remove(l.hyphen, l.hyphenWrap), s3.style.transform = `translateX(${r}px)`, o.style.transform = `translateX(${r}px)`, c.style.transform = `translateX(${m5}px)`, d.style.transform = `translateX(${m5}px)`;
  }
  /** Hides bounding tick labels that are obscured by either handle. */
  hideObscuredBoundingTickLabels() {
    const e = g(this.value);
    if (!this.hasHistogram && !e && !this.labelHandles && !this.precise || !this.hasHistogram && !e && this.labelHandles && !this.precise || !this.hasHistogram && !e && !this.labelHandles && this.precise || !this.hasHistogram && !e && this.labelHandles && this.precise || !this.hasHistogram && e && !this.precise || this.hasHistogram && !this.precise && !this.labelHandles)
      return;
    const t2 = this.el.shadowRoot.querySelector(`.${l.thumbMinValue}`), i3 = this.el.shadowRoot.querySelector(`.${l.thumbValue}`), a = this.el.shadowRoot.querySelector(`.${l.tickMin}`), s3 = this.el.shadowRoot.querySelector(`.${l.tickMax}`);
    !t2 && i3 && a && s3 && (a.style.opacity = this.isMinTickLabelObscured(a, i3) ? "0" : "1", s3.style.opacity = this.isMaxTickLabelObscured(s3, i3) ? "0" : "1"), t2 && i3 && a && s3 && (a.style.opacity = this.isMinTickLabelObscured(a, t2) || this.isMinTickLabelObscured(a, i3) ? "0" : "1", s3.style.opacity = this.isMaxTickLabelObscured(s3, t2) || this.isMaxTickLabelObscured(s3, i3) && this.hasHistogram ? "0" : "1");
  }
  /**
   * Returns an integer representing the number of pixels to offset on the left or right side based on desired position behavior.
   *
   * @param leftBounds
   * @param rightBounds
   * @private
   */
  getHostOffset(e, t2) {
    const { left: i3, right: a } = this.el.getBoundingClientRect();
    return e < i3 ? i3 - e : t2 > a ? -(t2 - a) : 0;
  }
  /**
   * Returns an integer representing the number of pixels that the two given span elements are overlapping, taking into account
   * a space in between the two spans equal to the font-size set on them to account for the space needed to render a hyphen.
   *
   * @param leftLabel
   * @param rightLabel
   */
  getRangeLabelOverlap(e, t2) {
    const i3 = e.getBoundingClientRect(), a = t2.getBoundingClientRect(), s3 = this.getFontSizeForElement(e), n = i3.right + s3 - a.left;
    return Math.max(n, 0);
  }
  /**
   * Returns a boolean value representing if the minLabel span element is obscured (being overlapped) by the given handle div element.
   *
   * @param minLabel
   * @param handle
   */
  isMinTickLabelObscured(e, t2) {
    const i3 = e.getBoundingClientRect(), a = t2.getBoundingClientRect();
    return Et(i3, a);
  }
  /**
   * Returns a boolean value representing if the maxLabel span element is obscured (being overlapped) by the given handle div element.
   *
   * @param maxLabel
   * @param handle
   */
  isMaxTickLabelObscured(e, t2) {
    const i3 = e.getBoundingClientRect(), a = t2.getBoundingClientRect();
    return Et(i3, a);
  }
  internalLabelFormatter(e, t2) {
    const i3 = this.labelFormatter;
    if (!i3)
      return this.formatValue(e);
    const a = i3(e, t2, this.formatValue);
    return a ?? this.formatValue(e);
  }
  // #endregion
  // #region Rendering
  render() {
    const e = this.el.id || this.guid, t2 = g(this.value) ? this.maxValue : this.value, i3 = this.minValue || this.min, a = this.shouldUseMinValue(), s3 = this.getUnitInterval(a ? this.minValue : i3) * 100, n = this.getUnitInterval(t2) * 100, o = this.shouldMirror(), r = g(this.value), c = this.buildThumbType("max"), u = this.renderThumb({
      type: c,
      thumbPlacement: c.includes("histogram") ? "below" : "above",
      maxInterval: n,
      minInterval: s3,
      mirror: o
    }), d = this.buildThumbType("min"), m5 = r ? this.renderThumb({
      type: d,
      thumbPlacement: d.includes("histogram") || d.includes("precise") ? "below" : "above",
      maxInterval: n,
      minInterval: s3,
      mirror: o
    }) : null, v2 = r ? "start" : this.fillPlacement, p2 = v2 === "none" ? {
      left: "unset",
      right: "unset"
    } : v2 === "end" ? {
      left: `${o ? s3 : n}%`,
      right: `${o ? n : s3}%`
    } : (
      /* default */
      {
        left: `${o ? 100 - n : s3}%`,
        right: `${o ? s3 : 100 - n}%`
      }
    );
    return setAttribute(this.el, "id", e), p({ disabled: this.disabled, children: html`<div aria-errormessage=${D2.validationMessage} .ariaInvalid=${this.status === "invalid"} .ariaLabel=${I(this)} class=${safeClassMap({
      [l.container]: true,
      [l.containerRange]: r,
      [`scale--${this.scale}`]: true
    })}>${this.renderGraph()}<div class=${safeClassMap(l.track)} ${ref(this.storeTrackRef)}><div class=${safeClassMap(l.trackRange)} @pointerdown=${this.onTrackPointerDown} style=${safeStyleMap(p2)}></div><div class=${safeClassMap(l.ticks)}>${this.tickValues.map((x) => {
      const b = `${this.getUnitInterval(x) * 100}%`;
      let h = false;
      if (v2 === "start" || v2 === "end")
        if (a)
          h = x >= this.minValue && x <= this.maxValue;
        else {
          const V = v2 === "start" ? i3 : t2, y = v2 === "start" ? t2 : this.max;
          h = x >= V && x <= y;
        }
      return html`<span class=${safeClassMap({
        [l.tick]: true,
        [l.tickActive]: h
      })} style=${safeStyleMap({
        left: o ? "" : b,
        right: o ? b : ""
      })}>${this.renderTickLabel(x)}</span>`;
    })}</div></div><div class=${safeClassMap(l.thumbContainer)}>${m5}${u}${B({ component: this })}</div></div>${this.validationMessage && this.status === "invalid" ? m3({ icon: this.validationIcon, id: D2.validationMessage, message: this.validationMessage, scale: this.scale, status: this.status }) : null}` });
  }
  renderThumb({ type: e, mirror: t2, thumbPlacement: i3, minInterval: a, maxInterval: s3 }) {
    const n = e.includes("labeled"), o = e.includes("precise"), r = e.includes("min"), c = g(this.value), u = r ? this.minValue : c ? this.maxValue : this.value, d = r ? "minValue" : c ? "maxValue" : "value", m5 = r ? this.minLabel : c ? this.maxLabel : this.minLabel, v2 = r ? this.minValue : u, p2 = d === "minValue" ? this.internalLabelFormatter(this.minValue, "min") : d === "maxValue" ? this.internalLabelFormatter(this.maxValue, "max") : this.internalLabelFormatter(u, "value"), x = r ? { left: `${t2 ? 100 - a : a}%` } : { right: `${t2 ? s3 : 100 - s3}%` }, b = `${l.handleLabel} ${r ? l.handleLabelMinValue : l.handleLabelValue}`, V = [
      ...n ? [
        html`<span aria-hidden=true class=${safeClassMap(b)}>${p2}</span>`,
        html`<span aria-hidden=true class=${`${b} ${l.static}`}>${p2}</span>`,
        html`<span aria-hidden=true class=${`${b} ${l.transformed}`}>${p2}</span>`
      ] : [],
      html`<div class=${safeClassMap(l.handle)}></div>`,
      o && html`<div class=${safeClassMap(l.handleExtension)}></div>` || ""
    ];
    return i3 === "below" && V.reverse(), keyed(e, html`<div .ariaLabel=${m5} aria-orientation=horizontal .ariaValueMax=${this.max} .ariaValueMin=${this.min} .ariaValueNow=${v2} class=${safeClassMap({
      [l.thumb]: true,
      [l.thumbValue]: !r,
      [l.thumbActive]: this.lastDragProp !== "minMaxValue" && this.dragProp === d,
      [l.thumbPrecise]: o,
      [l.thumbMinValue]: r
    })} data-value-prop=${d ?? nothing} @blur=${this.onThumbBlur} @focus=${this.onThumbFocus} @pointerdown=${this.onThumbPointerDown} role=slider style=${safeStyleMap(x)} tabindex=0 ${ref(this.storeThumbRef)}>${V}</div>`);
  }
  renderGraph() {
    return this.histogram ? html`<calcite-graph class=${safeClassMap(l.graph)} .colorStops=${this.histogramStops} .data=${this.histogram} .highlightMax=${g(this.value) ? this.maxValue : this.value} .highlightMin=${g(this.value) ? this.minValue : this.min} .max=${this.max} .min=${this.min}></calcite-graph>` : null;
  }
  renderTickLabel(e) {
    const { hasHistogram: t2, labelHandles: i3, labelTicks: a, max: s3, min: n, precise: o, value: r } = this, c = g(r), u = e === n, d = e === s3, m5 = u || d;
    return a && (!t2 && (m5 || !o || !c) || t2 && (m5 || !o && !i3)) ? html`<span class=${safeClassMap({
      [l.tickLabel]: true,
      [l.tickMin]: u,
      [l.tickMax]: d
    })}>${this.internalLabelFormatter(e, "tick")}</span>` : null;
  }
};
_se.properties = { maxValueDragRange: 16, minMaxValueRange: 16, minValueDragRange: 16, tickValues: 16, disabled: 7, fillPlacement: 3, form: 3, groupSeparator: 7, hasHistogram: 7, histogram: 0, histogramStops: 0, labelFormatter: 0, labelHandles: 7, labelTicks: 7, max: 11, maxLabel: 1, maxValue: 9, min: 11, minLabel: 1, minValue: 9, mirrored: 7, name: 3, numberingSystem: 1, pageStep: 11, precise: 7, required: 7, scale: 3, snap: 7, status: 3, step: 11, ticks: 11, validationIcon: [3, { converter: stringOrBoolean }], validationMessage: 1, validity: 0, value: 11 };
_se.shadowRootOptions = { mode: "open", delegatesFocus: true };
_se.styles = ie;
var se = _se;
S("calcite-slider", se);
export {
  se as Slider
};
/*! Bundled license information:

@esri/calcite-components/dist/components/calcite-slider/customElement.js:
  (*! All material copyright ESRI, All Rights Reserved, unless otherwise specified.
  See https://github.com/Esri/calcite-design-system/blob/dev/LICENSE.md for details.
  v3.1.0 *)
*/
//# sourceMappingURL=customElement-PG5F3N4F.js.map
